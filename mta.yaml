_schema-version: 3.3.0
ID: REGISTRATION_APP
description: A simple CAP project.
version: 1.0.0
modules:
- name: REGISTRATION_APP-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: REGISTRATION_APP-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
    memory: 500M
  build-parameters:
    builder: npm-ci
- name: REGISTRATION_APP-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: REGISTRATION_APP-db
  parameters:
    buildpack: nodejs_buildpack
    memory: 500M
- name: REGISTRATION_APP
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: REGISTRATION_APP-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: REGISTRATION_APP-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: REGISTRATION_APP_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - registrationlr.zip
      name: registrationlr
      target-path: resources/
- name: registrationlr
  type: html5
  path: app/registrationlr
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: REGISTRATION_APP-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: REGISTRATION_APP_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: REGISTRATION_APP-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: REGISTRATION_APP-destination-service
    service-plan: lite
  requires:
  - name: srv-api
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
